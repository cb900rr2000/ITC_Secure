# Analytics Rules build and deploy pipeline
# This pipeline publishes the rules file as an artifact and then uses a powershell task to deploy

parameters:
- name: customer
  displayName: Customer
  type: string
  default: DEV_Custom_No-Selection
  values:
  - DEV_Custom_Coller_Capital
  - DEV_Custom_Mizuho_International
  - DEV_Custom_DAC_Beachcroft
  - DEV_Custom_Gov_of_Jersey
  - DEV_Custom_Park_Square_Capital
  - DEV_Custom_3i_Group
  - DEV_Custom_C5_Capital
  - DEV_Custom_Essentra
  - DEV_Custom_Oxera_Consulting
  - DEV_Custom_Doctors_Laboratory
  - DEV_Custom_ITC_Secure
  - DEV_Custom_Annington
  - DEV_Custom_No-Selection

- name: AM-PM
  displayName: Alert Rule Shedule Period
  type: string
  default: No-Slection
  values:
  - AM
  - PM
  - No-Selection

name: build and deploy Alert Rules
resources:
 pipelines:
   - pipeline: Scripts
     source: 'scriptsCI'
trigger: false
 paths:
   include:
     - Alert_Rules/* 

stages:
- stage: build_alert_rules

  jobs:
    - job: AgentJob
      pool:
       name: Azure Pipelines
       vmImage: 'vs2017-win2016'
      steps:
       - task: CopyFiles@2
         displayName: 'Copy Alert Rules'
         inputs:
          SourceFolder: Alert_Rules/Custom_Individual_${{parameters.AM-PM}}/${{parameters.customer}}
          TargetFolder: '$(Pipeline.Workspace)'
       - task: Files-Validator@1
         inputs:
           rootDir: '$(Pipeline.Workspace)/*.json'
           validateXML: false
           validateJSON: true
           validateYAML: false
           validatePS: false
       - task: PublishBuildArtifacts@1
         displayName: 'Publish Artifact: RulesFile'
         inputs:
          PathtoPublish: '$(Pipeline.Workspace)'
          ArtifactName: RulesFile

- stage: Deploy_${{parameters.customer}}
  jobs:
    - job: AgentJob
      pool:
       name: Azure Pipelines
       vmImage: 'windows-2019'
      variables: 
      - group: ${{parameters.customer}}
      steps:
      - download: current
        artifact: RulesFile
      - download: Scripts
        patterns: '*.ps1'
      - task: AzurePowerShell@4
        displayName: 'Create and Update Alert Rules'
        inputs:
         azureSubscription: 'DEV_Custom_SPN'
         ScriptPath: '$(Pipeline.Workspace)/Scripts/Scripts/CreateAnalyticsRules.ps1'
         ScriptArguments: '-Workspace $(Workspace)'
         azurePowerShellVersion: LatestVersion
         pwsh: true